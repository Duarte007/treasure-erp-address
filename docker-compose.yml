version: '3.8'
services:
  db_treasure_address:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: duarte
      POSTGRES_PASSWORD: duartetcc
      POSTGRES_DB: treasure_address
    ports:
      - '5432:5432'
